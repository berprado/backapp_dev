# Este archivo inicializa el paquete 'database'

# Descomentar a medida que se implementen los modelos y el manager:
# from .models import Base, Empresa, Factura, EventoContingencia, init_db, get_db, SessionLocal, engine
# from .database_manager import DatabaseManager, db_manager_instance

# __all__ = [
#     "Base", "Empresa", "Factura", "EventoContingencia",
#     "init_db", "get_db", "SessionLocal", "engine",
#     "DatabaseManager", "db_manager_instance",
# ]
